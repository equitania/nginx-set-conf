---
phase: 03-tech-debt-repository-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nginx_set_conf/config_templates.py
  - nginx_set_conf/__init__.py
  - nginx_set_conf/nginx_set_conf.py
  - RELEASE_NOTES.md
autonomous: true
requirements:
  - TD-02

must_haves:
  truths:
    - "nginx_set_conf/config_templates.py does not exist in the repository"
    - "nginx_set_conf.py imports get_config_template from nginx_set_conf.templates.all_templates directly"
    - "No print() side-effects occur when a template is fetched at runtime"
    - "RELEASE_NOTES.md documents the removal of config_templates.py as a migration note"
    - "Full test suite passes after the removal"
  artifacts:
    - path: "nginx_set_conf/nginx_set_conf.py"
      provides: "Updated import line (line 26)"
      contains: "from nginx_set_conf.templates.all_templates import get_config_template"
    - path: "RELEASE_NOTES.md"
      provides: "Migration note for v1.12 deprecation-to-removal"
      contains: "config_templates"
  key_links:
    - from: "nginx_set_conf/nginx_set_conf.py"
      to: "nginx_set_conf/templates/all_templates.py"
      via: "direct import of get_config_template"
      pattern: "from nginx_set_conf\\.templates\\.all_templates import get_config_template"
---

<objective>
Hard-remove the deprecated config_templates.py shim and repoint the single
import in nginx_set_conf.py directly to all_templates.get_config_template.
Eliminating the shim removes the eager module-level evaluation of
config_template_dict that fired a print() side-effect on every template fetch.
Add a RELEASE_NOTES.md migration note for the deprecation → removal step.

Purpose: Retire dead code that polluted stdout, per decisions D-01/D-02/D-03/D-04.
Output: config_templates.py deleted; nginx_set_conf.py imports directly from
all_templates; __init__.py no longer re-exports config_templates; RELEASE_NOTES
entry added; test suite green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/PROJECT.md
@/Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/STATE.md
@/Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/phases/NSC-03-tech-debt-repository-cleanup/03-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Read from live files. -->

From nginx_set_conf/templates/all_templates.py (line 122):
  def get_config_template(config_template_name, domain=None) -> str
  Returns raw template string; empty string for unknown names.
  Signature is identical to the legacy wrapper in config_templates.py.

From nginx_set_conf/__init__.py (current):
  from . import config_templates as config_templates   ← must be removed
  from . import utils as utils
  from . import validators as validators
  __version__ = "1.11.1"

From nginx_set_conf/nginx_set_conf.py (line 26, current):
  from .config_templates import get_config_template   ← must be repointed

Call site (nginx_set_conf.py ~line 337):
  template_content = get_config_template(config_template)
  — behaviour is unchanged; only the import origin changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete config_templates.py and repair all import references</name>
  <files>
    nginx_set_conf/config_templates.py,
    nginx_set_conf/__init__.py,
    nginx_set_conf/nginx_set_conf.py,
    tests/test_templates.py
  </files>
  <read_first>
    nginx_set_conf/config_templates.py — confirm it only provides get_config_template; no other symbol is exported and used elsewhere.
    nginx_set_conf/__init__.py — current re-export line to remove.
    nginx_set_conf/nginx_set_conf.py lines 1-35 — full import block to edit.
    tests/test_templates.py lines 1-16 — test file imports from config_templates that must be updated.
  </read_first>
  <behavior>
    - Test: importing nginx_set_conf.config_templates raises ModuleNotFoundError after deletion.
    - Test: importing nginx_set_conf.nginx_set_conf succeeds without error and get_config_template is callable.
    - Test: get_config_template("odoo_ssl") from all_templates returns a non-empty string (regression: call still works).
    - Test: get_config_template("nonexistent") returns "".
  </behavior>
  <action>
    Step 1 — Update tests/test_templates.py: replace the import on line 7
    (from nginx_set_conf.config_templates import get_config_template) with
    (from nginx_set_conf.templates.all_templates import get_config_template).
    Remove the alias import on lines 13-15 (get_config_template as all_get_config_template) —
    after the change, both names refer to the same function; consolidate to a single import.
    Also update any test method that asserts ngx_-prefixed template names (test_backward_compat_ngx_prefix
    at line 51) — the ngx_ prefix was a feature of the legacy dict in config_templates.py; the test
    must be updated to assert that the canonical name "odoo_ssl" still resolves, since the ngx_ compat
    layer is going away with the shim.

    Step 2 — Update nginx_set_conf/__init__.py: remove the line
    (from . import config_templates as config_templates).
    Keep the utils and validators re-exports unchanged. Do NOT bump __version__ in this task —
    version bumping is done in Task 2 with the RELEASE_NOTES entry.

    Step 3 — Update nginx_set_conf/nginx_set_conf.py line 26: replace
    (from .config_templates import get_config_template) with
    (from .templates.all_templates import get_config_template).
    No other lines in this file change — the call site at ~line 337 is unchanged.

    Step 4 — Delete nginx_set_conf/config_templates.py using the Bash tool:
    git rm nginx_set_conf/config_templates.py
    (git rm stages the deletion so it is tracked; do NOT use a plain rm).

    The result: no file named config_templates.py exists anywhere under nginx_set_conf/;
    all imports resolve through all_templates directly.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && python -c "from nginx_set_conf.templates.all_templates import get_config_template; assert get_config_template('odoo_ssl') != ''; print('import OK')"</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && python -c "import nginx_set_conf.config_templates" 2>&1 | grep -q "ModuleNotFoundError" && echo "deletion confirmed"</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && python -m pytest tests/test_templates.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - nginx_set_conf/config_templates.py does not exist on disk and is not present in git ls-files output.
    - nginx_set_conf/nginx_set_conf.py line 26 contains exactly: from .templates.all_templates import get_config_template
    - nginx_set_conf/__init__.py does NOT contain the string "config_templates".
    - tests/test_templates.py does NOT import from nginx_set_conf.config_templates.
    - pytest tests/test_templates.py exits 0 with no failures.
  </acceptance_criteria>
  <done>config_templates.py removed; all imports repaired; test_templates.py updated; test suite green for that module.</done>
</task>

<task type="auto">
  <name>Task 2: Add RELEASE_NOTES.md migration note for config_templates removal</name>
  <files>
    RELEASE_NOTES.md
  </files>
  <read_first>
    RELEASE_NOTES.md lines 1-60 — read the existing v1.11.1 entry format so the new v1.12 section follows the same structure (heading, [CHG] prefix, "Why this matters", "Migration" subsection).
  </read_first>
  <action>
    Prepend a new "## Version 1.12.0 (development)" section at the top of RELEASE_NOTES.md,
    before the existing "## Version 1.11.1" entry. Follow the established format exactly:
    - Heading: ## Version 1.12.0 (development)
    - Subheading: ### Changed (tech-debt cleanup)
    - Bullet: [CHG] config_templates.py removed — the internal shim module
      nginx_set_conf/config_templates.py has been hard-deleted. It was marked deprecated
      since v1.11.x. The module had no documented external Python consumers; it existed
      only to provide a backward-compat wrapper around nginx_set_conf.templates.all_templates.
    - Subheading: ### Migration
    - Paragraph: If any in-house code imported from nginx_set_conf.config_templates,
      update the import to nginx_set_conf.templates.all_templates.get_config_template.
      The function signature (config_template_name: str, domain: str | None = None) -> str
      is identical. Operator YAML configs are unaffected — this change is internal to the
      Python import surface only.

    Do NOT add a version/date line to any source file header in this task — version bump
    is a separate Phase 4 concern.
  </action>
  <verify>
    <automated>grep -c "config_templates" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/RELEASE_NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - RELEASE_NOTES.md contains the string "config_templates.py removed" (exact phrase).
    - RELEASE_NOTES.md contains "## Version 1.12.0" as a top-level heading.
    - The new section appears before "## Version 1.11.1" in the file.
    - The "Migration" subsection mentions nginx_set_conf.templates.all_templates.
  </acceptance_criteria>
  <done>RELEASE_NOTES.md has a v1.12.0 (development) section with config_templates removal note and migration instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python import graph | Removing config_templates.py changes which module provides get_config_template |
| RELEASE_NOTES | Public-facing changelog — must accurately describe what changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | nginx_set_conf/nginx_set_conf.py import line | mitigate | Task 1 explicitly repoints import to all_templates; test asserts get_config_template("odoo_ssl") != "" post-change |
| T-03-01-02 | Information Disclosure | print() side-effects from config_template_dict eager eval | mitigate | Removing config_templates.py eliminates the entire eager-eval dict; no print() remains in the template-fetch path |
| T-03-01-03 | Repudiation | RELEASE_NOTES.md omits migration guidance | mitigate | Task 2 mandates Migration subsection with exact import path |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all changes are file edits and a git rm |

Security posture note: Removing config_templates.py has no effect on the sentinel-leak or
log-path guards from Phase 2 — those guards live in utils.py (redirect_domain validator) and
are not touched by this plan. The import-surface change is purely internal.
</threat_model>

<verification>
After both tasks complete, run the full test suite:

  cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && python -m pytest -x -q

All tests must pass. Additionally:
  git ls-files nginx_set_conf/config_templates.py   — must return empty
  grep -rn "config_templates" nginx_set_conf/        — must return no matches
</verification>

<success_criteria>
1. nginx_set_conf/config_templates.py does not exist (deleted via git rm).
2. nginx_set_conf/nginx_set_conf.py line 26: from .templates.all_templates import get_config_template
3. nginx_set_conf/__init__.py has no reference to config_templates.
4. tests/test_templates.py has no import from nginx_set_conf.config_templates.
5. RELEASE_NOTES.md has ## Version 1.12.0 (development) with a migration note.
6. pytest exits 0.
</success_criteria>

<output>
Create .planning/phases/NSC-03-tech-debt-repository-cleanup/03-01-SUMMARY.md when done.
</output>

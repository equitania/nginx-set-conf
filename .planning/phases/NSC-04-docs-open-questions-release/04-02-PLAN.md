---
phase: 04-docs-open-questions-release
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/codebase/CONCERNS.md
  - RELEASE_NOTES.md
  - pyproject.toml
  - nginx_set_conf/__init__.py
autonomous: false
requirements:
  - DOC-01
  - Q-01
  - Q-02
must_haves:
  truths:
    - "CONCERNS.md shows zero HIGH/MEDIUM findings after Phases 1–4 land"
    - "RELEASE_NOTES.md v1.12.0 has a release date (not 'development') and complete Phase 4 entries"
    - "pyproject.toml version is 1.12.0"
    - "nginx_set_conf/__init__.py __version__ is 1.12.0"
    - "git tag v1.12.0 is created by bump-my-version"
    - "Tag is pushed to origin and upstream by the operator"
    - "uv build + uvpublish run locally by the operator"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Re-audited findings with zero HIGH/MEDIUM open"
      contains: "Phase 4 complete"
    - path: "RELEASE_NOTES.md"
      provides: "Finalised v1.12.0 section with date and all Phase 4 entries"
      contains: "1.12.0"
    - path: "pyproject.toml"
      provides: "Version bumped to 1.12.0"
      contains: "1.12.0"
    - path: "nginx_set_conf/__init__.py"
      provides: "Version bumped to 1.12.0"
      contains: "1.12.0"
  key_links:
    - from: "pyproject.toml"
      to: "nginx_set_conf/__init__.py"
      via: "bump-my-version sync"
      pattern: "1\\.12\\.0"
    - from: "RELEASE_NOTES.md"
      to: "git tag v1.12.0"
      via: "operator tag push"
      pattern: "v1.12.0"
---

<objective>
Re-audit CONCERNS.md to confirm zero HIGH/MEDIUM findings remain after Phases 1–4. Finalise RELEASE_NOTES.md v1.12.0 (drop the "(development)" marker, set the release date, add test count and coverage updates). Run `bump-my-version bump minor` to advance 1.11.1 → 1.12.0 and create the release tag. The tag push and PyPI publish are operator-run steps (autonomous: false).

Purpose: Ship v1.12.0. Every hardening requirement from the 2026-05-28 audit is now implemented — this plan confirms it and cuts the release.
Output: Updated CONCERNS.md (re-audit annotation), finalised RELEASE_NOTES.md, version files at 1.12.0, git tag v1.12.0 created. Tag push + PyPI publish operator-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NSC-04-docs-open-questions-release/04-CONTEXT.md
@.planning/phases/NSC-04-docs-open-questions-release/04-01-SUMMARY.md
</context>

<interfaces>
From pyproject.toml [tool.bump-my-version]:
  current_version = "1.11.1"
  commit = true
  tag = true
  tag_name = "v{new_version}"
  files: nginx_set_conf/__init__.py and pyproject.toml

From nginx_set_conf/__init__.py:
  __version__ = "1.11.1"

From CONCERNS.md structure:
  Sections: Security Concerns, Correctness Concerns, Tech Debt / Smells, Open Questions / TODOs, Documentation Drift.
  Each finding has a severity marker: [HIGH], [MEDIUM], [LOW], [QUESTION].
  Phases 1–3 addressed: SEC-01..04 (Phase 1), COR-01..05 (Phase 2), TD-01..06 (Phase 3), PROTO-01 (Phase 2.5).
  Phase 4 addresses: DOC-01, Q-01, Q-02.

From RELEASE_NOTES.md current v1.12.0 header:
  "## Version 1.12.0 (development)"
  (development marker to be dropped; release date to be added in DD.MM.YYYY format)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-audit CONCERNS.md — confirm zero HIGH/MEDIUM open findings</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file — all findings, their severities, and which phases addressed them)
    - .planning/ROADMAP.md (Phase 1–4 success criteria to verify against)
    - .planning/phases/NSC-04-docs-open-questions-release/04-01-SUMMARY.md (confirms Phase 4 work is done)
    - .planning/phases/03-tech-debt-repository-cleanup/*-SUMMARY.md (Phase 3 confirmation)
  </read_first>
  <action>
    Read CONCERNS.md and cross-reference each finding against completed phases:

    HIGH findings (3 total from audit):
    - [HIGH] nginx restart before config validation → Phase 1 / v1.11.1 fixed (pre-reload nginx -t gate)
    - [HIGH] auth_file template injection → Phase 1 / v1.11.1 fixed (validate_auth_file constrains to /etc/nginx/)
    - [HIGH] wildcard domain glob filename → Phase 1 / v1.11.1 fixed (_safe_conf_filename)
    All three are already closed in v1.11.1 (pre-GSD sprint). Mark CONFIRMED CLOSED.

    MEDIUM findings (5 total):
    - [MEDIUM] target_path whitespace bypass → SEC-01 / Phase 1 → CLOSED
    - [MEDIUM] backup symlink copytree → SEC-02 / Phase 1 → CLOSED
    - [MEDIUM] key file world-readable window → SEC-03 / Phase 1 → CLOSED
    - [MEDIUM] dual cache-path pipeline → COR-01 / Phase 2 → CLOSED
    - [MEDIUM] redirect_ssl sentinel in log paths → COR-02 / Phase 2 → CLOSED
    - [MEDIUM] proxy_cache_path /tmp sentinel → COR-03 / Phase 2 → CLOSED (CACHE_PATH_SENTINEL)
    - [MEDIUM] HTTP/2 banner without directive → PROTO-01 / Phase 2.5 → CLOSED
    - [MEDIUM] config_templates.py shim → TD-02 / Phase 3 → CLOSED
    - [MEDIUM] egg-info dual identity → TD-01 / Phase 3 → CLOSED
    All MEDIUM findings CONFIRMED CLOSED.

    LOW findings:
    - SEC-LOW-1: retrieve_valid_input recursion → SEC-04 / Phase 1 → CLOSED
    - COR-LOW-1: default_ssl_reject absent from VALID_TEMPLATES → COR-04 / Phase 2 → CLOSED (documented inline)
    - COR-LOW-2: f-string back-reference repl → COR-05 / Phase 2 → CLOSED (lambda repl)
    - TD-LOW-1: nginx_set_conf.log committed → TD-03 / Phase 3 → CLOSED
    - TD-LOW-2: build/ dist/ gitignore → TD-04 / Phase 3 → CLOSED (confirmed untracked)
    - TD-LOW-3: redirect templates unused directives → TD-05 / Phase 3 → CLOSED
    - TD-LOW-4: interactive mode cert_key → TD-06 / Phase 3 → CLOSED (documented in --help)
    - DOC-LOW-1: CLAUDE.md replace_cache_path wrong file → DOC-01 / Phase 4 → CLOSED (this phase)

    QUESTION findings:
    - Q-1: --migrate_to_ip_bound teaser → Q-01 / Phase 4 → CLOSED (teaser removed, manual procedure documented)
    - Q-2: --sync_config silent overwrite → Q-02 / Phase 4 → CLOSED (--force gate implemented)

    Edit CONCERNS.md: At the top, add a re-audit header block:
      "## Re-Audit: Phase 4 Complete (29.05.2026)"
      "All HIGH and MEDIUM findings closed by Phases 1–4 of the v1.12 roadmap."
      "Zero HIGH / Zero MEDIUM open findings."
      "LOW findings: all closed (see phase summaries above)."
      "Open Questions: both resolved (Q-01, Q-02)."

    Then annotate each finding in the existing sections with a one-line "(CLOSED — Phase N / Req-ID)" suffix. Do NOT delete existing finding text — operators may need the historical description.
  </action>
  <verify>
    <automated>grep -c "\[HIGH\]" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/codebase/CONCERNS.md</automated>
    <automated>grep -c "\[MEDIUM\]" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/codebase/CONCERNS.md</automated>
    <automated>grep "Re-Audit" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/.planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Re-Audit" CONCERNS.md returns the new header line
    - Every [HIGH] and [MEDIUM] line in CONCERNS.md has a "(CLOSED" annotation on its line or the next line
    - grep "CLOSED" CONCERNS.md returns at least 12 lines (3 HIGH + 9 MEDIUM-class = 12 minimum)
    - The re-audit header section states "Zero HIGH / Zero MEDIUM open findings"
  </acceptance_criteria>
  <done>CONCERNS.md is re-audited. Every HIGH and MEDIUM finding is annotated CLOSED with its phase and requirement ID. The re-audit header confirms zero open HIGH/MEDIUM findings after Phases 1–4.</done>
</task>

<task type="auto">
  <name>Task 2: Finalise RELEASE_NOTES.md v1.12.0 and run bump-my-version</name>
  <files>RELEASE_NOTES.md, pyproject.toml, nginx_set_conf/__init__.py</files>
  <read_first>
    - RELEASE_NOTES.md (full file — v1.12.0 section at the top; note the "(development)" marker to drop and missing entries to add)
    - pyproject.toml (lines 114-129 — bump-my-version configuration; confirm current_version = "1.11.1")
    - nginx_set_conf/__init__.py (full file — confirm __version__ = "1.11.1" before bump)
    - .planning/phases/NSC-04-docs-open-questions-release/04-01-SUMMARY.md (test count and coverage after Phase 4 tasks, to update the test count in RELEASE_NOTES)
  </read_first>
  <action>
    Step 1 — Edit RELEASE_NOTES.md before the version bump (bump-my-version does not touch RELEASE_NOTES):
    - Replace "## Version 1.12.0 (development)" with "## Version 1.12.0 (29.05.2026)" (use the current date from environment: 2026-05-29, format DD.MM.YYYY → 29.05.2026).
    - Add the Phase 4 entries to the v1.12.0 section. The section currently contains only the config_templates.py removal. Add:
      - "### Documentation" subsection with the DOC-01 fix (CLAUDE.md __init__.py → all_templates.py correction).
      - "### Q-01: --migrate_to_ip_bound not implemented" entry (already written in 04-01 Task 1 — verify it is present; if not, add it).
      - "### Q-02: --sync_config now requires --force" entry (same verification / add if absent).
      - A "### Tests" line recording the updated test count (read from 04-01-SUMMARY.md or from `pytest tests/ --co -q | tail -3` dry output if available).
    - Add a "### Migration" note that v1.12.0 is a drop-in upgrade from v1.11.1; the only operator-visible behaviour change is that --sync_config now requires --force when server files differ.

    Step 2 — Run bump-my-version to advance 1.11.1 → 1.12.0:
      bump-my-version bump minor
    This command:
    - Updates pyproject.toml version field to 1.12.0
    - Updates nginx_set_conf/__init__.py __version__ to 1.12.0
    - Creates a git commit (message per bump-my-version defaults)
    - Creates git tag v1.12.0

    CRITICAL: Do NOT push the tag. Tag push is operator-run (checkpoint task below).
  </action>
  <verify>
    <automated>grep "version" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/pyproject.toml | head -5</automated>
    <automated>grep "__version__" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/nginx_set_conf/__init__.py</automated>
    <automated>grep "1.12.0" /Users/picard/gitbase/PyPi-Projects/nginx-set-conf/RELEASE_NOTES.md | head -3</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && git tag | grep v1.12.0</automated>
  </verify>
  <acceptance_criteria>
    - grep 'version = "1.12.0"' pyproject.toml returns one line
    - grep '__version__ = "1.12.0"' nginx_set_conf/__init__.py returns one line
    - RELEASE_NOTES.md first line of the v1.12.0 section is "## Version 1.12.0 (29.05.2026)" (no "(development)" marker)
    - git tag | grep v1.12.0 returns "v1.12.0"
    - Full test suite passes: pytest tests/ -v —— all green with the new version in __init__.py
  </acceptance_criteria>
  <done>RELEASE_NOTES.md v1.12.0 is finalised with date and all Phase 4 entries. pyproject.toml and __init__.py are at 1.12.0. git tag v1.12.0 exists locally.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Operator — push tag + run uv build + uvpublish</name>
  <what-built>
    bump-my-version created git commit + tag v1.12.0 locally. RELEASE_NOTES, version files, and CONCERNS re-audit are all committed. Full test suite is green.
  </what-built>
  <how-to-verify>
    Run the following commands as the operator (in order):

    1. Push to both remotes (as per project convention):
       git push origin 2026
       git push upstream 2026
       git push origin v1.12.0
       git push upstream v1.12.0

    2. Build the distribution:
       uv build

    3. Publish to PyPI (local publish, NOT via CI):
       uvpublish
       (or: uv publish dist/nginx_set_conf-1.12.0-py3-none-any.whl)

    4. Verify on PyPI that version 1.12.0 is visible:
       https://pypi.org/project/nginx-set-conf/

    Expected: v1.12.0 visible on PyPI within a few minutes of publish.
    NO CI publish step is added. This is the permanent local-only policy.
  </how-to-verify>
  <resume-signal>Type "published" once PyPI shows nginx-set-conf 1.12.0, or describe any issue encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → remote origin/upstream | Tag push sends signed release tag to public remotes |
| uv build → PyPI publish | Built wheel is uploaded to public PyPI under operator credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | git tag push to origin/upstream | accept | Tag is created by bump-my-version from a clean, tested commit; operator verifies before pushing |
| T-04-02-02 | Information Disclosure | RELEASE_NOTES.md public visibility | accept | Release notes intentionally public; no secrets or internal paths exposed |
| T-04-02-03 | Denial of Service | CONCERNS.md re-audit annotation | accept | Documentation edit; no executable surface |
| T-04-02-SC | Tampering | pip/uv install (uv build) | accept | No new packages installed in this plan; uv build uses already-installed packages; no new --install calls |

Note: This plan performs no new package installs and introduces no new external dependencies. The primary risk surface is the PyPI publish, which is gated as an operator-run checkpoint (autonomous: false).
</threat_model>

<verification>
After all tasks (including operator checkpoint):

1. grep 'version = "1.12.0"' pyproject.toml — returns one line
2. grep '__version__ = "1.12.0"' nginx_set_conf/__init__.py — returns one line
3. grep "Re-Audit" .planning/codebase/CONCERNS.md — returns the header line
4. grep -c "CLOSED" .planning/codebase/CONCERNS.md — returns 12 or more
5. head -1 RELEASE_NOTES.md | grep "1.12.0" — returns the version header (not "development")
6. grep "29.05.2026" RELEASE_NOTES.md — returns the release date line
7. git tag | grep v1.12.0 — returns v1.12.0
8. git log --oneline -3 — shows the bump-my-version commit
9. (Operator) PyPI page https://pypi.org/project/nginx-set-conf/ shows 1.12.0 as latest
</verification>

<success_criteria>
- CONCERNS.md re-audited: zero HIGH/MEDIUM open findings; every finding annotated CLOSED with phase + requirement ID.
- RELEASE_NOTES.md v1.12.0: "(development)" marker removed; release date 29.05.2026 set; DOC-01, Q-01, Q-02, and migration note present.
- pyproject.toml version = "1.12.0"; nginx_set_conf/__init__.py __version__ = "1.12.0".
- git tag v1.12.0 exists locally; operator has pushed to origin and upstream.
- PyPI shows nginx-set-conf 1.12.0 (operator-confirmed).
</success_criteria>

<output>
Create `.planning/phases/NSC-04-docs-open-questions-release/04-02-SUMMARY.md` when done.
</output>

---
phase: 05-http3-opt-in-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nginx_set_conf/nginx_set_conf.py
  - nginx_set_conf/validators.py
  - tests/test_validators.py
autonomous: true
requirements:
  - PROTO-02
  - PROTO-03

must_haves:
  truths:
    - "nginx-set-conf --enable_http3 on an included template passes validation without error (per D-02 PROTO-02)"
    - "--enable_http3 on any of the 6 excluded templates raises ValidationError naming the exclusion rationale (per D-03 PROTO-03)"
    - "enable_http3: true in a YAML config is read and forwarded to execute_commands exactly as disable_domain_listen is (per D-02)"
    - "HTTP3_EXCLUDED_TEMPLATES is a frozenset constant in validators.py adjacent to VALID_TEMPLATES (per D-03)"
    - "uv run pytest exits 0 after all changes"
  artifacts:
    - path: "nginx_set_conf/nginx_set_conf.py"
      provides: "--enable_http3 Click option + YAML read-through + execute_commands pass-through"
      contains: "enable_http3"
    - path: "nginx_set_conf/validators.py"
      provides: "HTTP3_EXCLUDED_TEMPLATES constant + exclusion guard in validate_all_inputs"
      contains: "HTTP3_EXCLUDED_TEMPLATES"
    - path: "tests/test_validators.py"
      provides: "TestHttp3Exclusion class with 4 test methods"
      contains: "TestHttp3Exclusion"
  key_links:
    - from: "nginx_set_conf/nginx_set_conf.py"
      to: "nginx_set_conf/validators.py"
      via: "validate_all_inputs(enable_http3=...)"
      pattern: "enable_http3"
    - from: "nginx_set_conf/nginx_set_conf.py"
      to: "nginx_set_conf/utils.py"
      via: "execute_commands(enable_http3=...)"
      pattern: "enable_http3"
---

<objective>
Wire the --enable_http3 opt-in flag end-to-end: CLI declaration → YAML read-through → execute_commands keyword arg → validate_all_inputs exclusion check.

Purpose: This is the plumbing layer. No directives are emitted yet (that is 05-02); this plan ensures the flag exists, propagates correctly, and is rejected for the 6 excluded templates before any file is written.

Output: Updated nginx_set_conf.py (flag + YAML + call-sites), updated validators.py (HTTP3_EXCLUDED_TEMPLATES constant + validate_all_inputs guard), new TestHttp3Exclusion class in test_validators.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-http3-opt-in-support/05-CONTEXT.md
@.planning/phases/05-http3-opt-in-support/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --enable_http3 to CLI, YAML read-through, and execute_commands signature</name>
  <files>nginx_set_conf/nginx_set_conf.py</files>

  <read_first>
    - nginx_set_conf/nginx_set_conf.py lines 197–215 (--disable_domain_listen option — exact shape to copy)
    - nginx_set_conf/nginx_set_conf.py lines 270–295 (start_nginx_set_conf function signature)
    - nginx_set_conf/nginx_set_conf.py lines 383–415 (YAML read-through block: yaml_disable_domain_listen pattern)
    - nginx_set_conf/nginx_set_conf.py lines 395–440 (both execute_commands call sites)
    - nginx_set_conf/utils.py lines 587–625 (execute_commands signature: current last positional params)
    - .planning/phases/05-http3-opt-in-support/05-PATTERNS.md §nginx_set_conf.py (full pattern excerpt)
    - ~/.claude/skills/nginx-set-conf/SKILL.md §DANGER ZONE (replace-order invariant)
  </read_first>

  <behavior>
    - After the change, `@click.option("--enable_http3", is_flag=True, ...)` exists in the decorator chain, positioned after the --backend_ip option and before the --config_path option group.
    - `start_nginx_set_conf(...)` has `enable_http3` as a parameter after `disable_domain_listen`.
    - The YAML read-through block contains `yaml_enable_http3 = yaml_config.get("enable_http3", False)`.
    - Both execute_commands call sites pass `enable_http3=yaml_enable_http3` (YAML path) and `enable_http3=enable_http3` (CLI path) as a keyword argument.
    - The interactive path (start_nginx_set_conf lines 450–480 interactive block) forwards `enable_http3` unchanged.
    - execute_commands signature in utils.py gains `enable_http3=False` as the last parameter after `backend_ip`.
    - NO HTTP/3 directives are emitted in this task — only plumbing.
  </behavior>

  <action>
    1. Add the @click.option for --enable_http3. Copy the --disable_domain_listen decorator shape verbatim. Use is_flag=True, default=False (implicit for is_flag). Help text: "Emit QUIC/HTTP/3 listen directives and Alt-Svc header for browser-facing SSL templates. Requires nginx >= 1.25.0 and UDP/443 open in the host firewall. See README for full prerequisites. Excluded templates: fast_report, mailpit, redirect, redirect_ssl, default_ssl_reject, odoo_http."

    2. Add `enable_http3` as a parameter to `start_nginx_set_conf` immediately after `disable_domain_listen`. Position in function signature must match the Click decorator order.

    3. In the YAML config read block (around line 386), add `yaml_enable_http3 = yaml_config.get("enable_http3", False)` immediately after the yaml_disable_domain_listen assignment.

    4. Update both execute_commands call sites to pass `enable_http3=yaml_enable_http3` (YAML/--config_path path) and `enable_http3=enable_http3` (CLI/interactive path). Add as keyword arg — never as positional.

    5. In utils.py `execute_commands` signature (line ~602), add `enable_http3=False` as the last parameter after `backend_ip=None`. Add it to the docstring parameter block. Pass it through to `validate_all_inputs` as `enable_http3=enable_http3` in the validate_all_inputs call (around line 641). Do NOT implement any emission logic yet — that is 05-02.

    REPLACE-ORDER INVARIANT (SKILL.md DANGER ZONE): The execute_commands replacement sequence — IP-placeholder → domain-placeholder → disable_domain_listen rewrite → backend-IP placeholder — must not be reordered. The enable_http3 injection (added in 05-02) will go AFTER disable_domain_listen and BEFORE backend-IP substitution. This task only wires the parameter; leave a TODO comment at the injection point: `# TODO(05-02): inject HTTP/3 directives here when enable_http3=True`.

    ip.ip.ip.ip CONSTRAINT: The placeholder `ip.ip.ip.ip` is reserved for listen lines only, never proxy_pass. Do not introduce it anywhere in this task (it is already present in templates; this task does not touch templates).
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && uv run pytest tests/test_validators.py tests/test_templates.py -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "enable_http3" nginx_set_conf/nginx_set_conf.py` returns >= 6 (option decl, param, yaml_get, 2 call sites, interactive forward)
    - `grep -c "enable_http3" nginx_set_conf/utils.py` returns >= 2 (signature + validate call)
    - `python -c "from nginx_set_conf.nginx_set_conf import start_nginx_set_conf; import inspect; sig=inspect.signature(start_nginx_set_conf); print('enable_http3' in sig.parameters)"` prints True
    - `python -c "from nginx_set_conf.utils import execute_commands; import inspect; sig=inspect.signature(execute_commands); print(sig.parameters['enable_http3'].default)"` prints False
    - uv run pytest tests/ -x -q exits 0 (no regressions in existing tests)
  </acceptance_criteria>

  <done>--enable_http3 flag exists in CLI and is forwarded from Click → start_nginx_set_conf → execute_commands → validate_all_inputs. No emission yet. Test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTTP3_EXCLUDED_TEMPLATES constant + validate_all_inputs guard + TestHttp3Exclusion</name>
  <files>nginx_set_conf/validators.py, tests/test_validators.py</files>

  <read_first>
    - nginx_set_conf/validators.py lines 32–60 (VALID_TEMPLATES definition — position and style for HTTP3_EXCLUDED_TEMPLATES)
    - nginx_set_conf/validators.py lines 333–385 (validate_all_inputs signature, parameter list, body — where to add enable_http3 param and guard)
    - nginx_set_conf/validators.py lines 355–365 (COR-02 redirect_domain guard — exact shape to copy for HTTP/3 exclusion guard)
    - tests/test_validators.py lines 337–410 (TestValidateAllInputs and TestValidateRedirectDomain — test structure to follow)
    - .planning/phases/05-http3-opt-in-support/05-PATTERNS.md §validators.py (full constant + guard + test excerpts)
    - .planning/phases/05-http3-opt-in-support/05-CONTEXT.md §Scope: which templates get HTTP/3
  </read_first>

  <behavior>
    - validators.py contains `HTTP3_EXCLUDED_TEMPLATES = frozenset({...})` with exactly these 6 members: "fast_report", "mailpit", "redirect", "redirect_ssl", "default_ssl_reject", "odoo_http". Each member has an inline rationale comment.
    - validate_all_inputs signature gains `enable_http3: bool = False` as the last parameter after `backend_ip: str = ""`.
    - When enable_http3=True and config_template is in HTTP3_EXCLUDED_TEMPLATES, validate_all_inputs raises ValidationError with a message containing "--enable_http3" and the template name and a list of HTTP/3-capable templates (VALID_TEMPLATES - HTTP3_EXCLUDED_TEMPLATES, sorted).
    - When enable_http3=False, the exclusion guard is never evaluated (fast path).
    - TestHttp3Exclusion class exists in test_validators.py with the 4 test methods described in 05-PATTERNS.md.
  </behavior>

  <action>
    1. In validators.py, immediately after the VALID_TEMPLATES block (after line ~60), add HTTP3_EXCLUDED_TEMPLATES as a frozenset constant. Include a block comment explaining the rationale per member, following the same comment style as the VALID_TEMPLATES block. Members (exactly as in CONTEXT.md decisions): "fast_report" (server-to-server PDF API, no browser traffic), "mailpit" (dev SMTP test tool, internal-only), "redirect" (HTTP-only, no TLS), "redirect_ssl" (trivial 301, QUIC overhead not worth it), "default_ssl_reject" (SNI catch-all returns 444, no useful response), "odoo_http" (HTTP-only, no TLS).

    2. Add `enable_http3: bool = False` as the last parameter to `validate_all_inputs` (after `backend_ip: str = ""`). Update the docstring.

    3. Add the HTTP/3 exclusion guard in `validate_all_inputs` body, AFTER the COR-02 redirect_domain guard and BEFORE the backend_ip validate_ip call. Shape: if enable_http3 and config_template in HTTP3_EXCLUDED_TEMPLATES: raise ValidationError with message "Template '{config_template}' does not support --enable_http3. HTTP/3-capable templates: {sorted list of VALID_TEMPLATES - HTTP3_EXCLUDED_TEMPLATES joined by ', '}".

    4. In tests/test_validators.py, add class TestHttp3Exclusion with these 4 test methods:
       - test_excluded_template_raises_validation_error: calls validate_all_inputs with config_template="fast_report", enable_http3=True; asserts ValidationError is raised with match="enable_http3".
       - test_included_template_passes: calls validate_all_inputs with config_template="odoo_ssl", domain="example.com", ip="1.2.3.4", port="8069", cert_name="example.com", enable_http3=True; asserts no exception.
       - test_all_excluded_templates_reject_http3: @pytest.mark.parametrize over all 6 excluded template names; each call with enable_http3=True must raise ValidationError. For "redirect" and "redirect_ssl", also supply redirect_domain="new.example.com".
       - test_http3_false_skips_exclusion_check: validate_all_inputs with config_template="fast_report", enable_http3=False must not raise.

    NOTE: The guard must also handle the case where config_template is validated after validate_config_template is called — validate_all_inputs calls validate_config_template early, so by the time the HTTP/3 guard runs the template name is already known to be valid. The guard simply checks membership in HTTP3_EXCLUDED_TEMPLATES; no second template-lookup needed.
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && uv run pytest tests/test_validators.py -x -v -k "Http3" 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from nginx_set_conf.validators import HTTP3_EXCLUDED_TEMPLATES; print(sorted(HTTP3_EXCLUDED_TEMPLATES))"` prints "['default_ssl_reject', 'fast_report', 'mailpit', 'odoo_http', 'redirect', 'redirect_ssl']"
    - `grep -c "HTTP3_EXCLUDED_TEMPLATES = frozenset" nginx_set_conf/validators.py` returns 1
    - `uv run pytest tests/test_validators.py -k "Http3" -v` shows 4 tests collected and all pass
    - `uv run pytest tests/ -x -q` exits 0 (full suite green)
    - Calling `validate_all_inputs(config_template="fast_report", domain="x.com", ip="1.2.3.4", port="8080", cert_name="x.com", enable_http3=True)` in a Python shell raises ValidationError with "fast_report" and "enable_http3" in the message.
    - Calling the same with enable_http3=False raises no exception.
  </acceptance_criteria>

  <done>HTTP3_EXCLUDED_TEMPLATES frozenset in validators.py, exclusion guard in validate_all_inputs, 4 TestHttp3Exclusion tests all green. Full suite exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config → CLI | User-controlled YAML supplies enable_http3 value; must be coerced to bool, never eval'd |
| CLI args → execute_commands | Click ensures is_flag is always bool; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | enable_http3 YAML value | mitigate | yaml_config.get("enable_http3", False) returns Python bool via PyYAML; no eval; truthy strings do NOT reach execute_commands (YAML bool only) — validate in test |
| T-05-01-02 | Elevation of Privilege | HTTP3_EXCLUDED_TEMPLATES bypass | mitigate | frozenset is immutable; guard evaluated before any file write; test covers all 6 exclusions |
| T-05-01-03 | Information Disclosure | Error message leaks template list | accept | The HTTP/3-capable template list is not sensitive; it is already in public --help output |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; uses existing click, pytest, validators |
</threat_model>

<verification>
- `uv run pytest tests/ -x -q` exits 0
- `python -c "from nginx_set_conf.validators import HTTP3_EXCLUDED_TEMPLATES; assert len(HTTP3_EXCLUDED_TEMPLATES) == 6"` exits 0
- `python -c "from nginx_set_conf.utils import execute_commands; import inspect; p=inspect.signature(execute_commands).parameters; assert 'enable_http3' in p and p['enable_http3'].default == False"` exits 0
- `grep -v '^#' nginx_set_conf/validators.py | grep -c 'HTTP3_EXCLUDED_TEMPLATES'` returns >= 2 (definition + guard usage)
</verification>

<success_criteria>
- --enable_http3 is a recognized Click flag; `nginx-set-conf --help` lists it.
- `enable_http3: true` in a YAML config is consumed without KeyError.
- validate_all_inputs rejects enable_http3=True for each of the 6 excluded templates with a ValidationError naming the template and "enable_http3".
- validate_all_inputs accepts enable_http3=True for odoo_ssl, flowise, n8n, nextcloud, guacamole, kasm, pgadmin, portainer, pwa, code_server, supabase, qdrant without error.
- Full pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/05-http3-opt-in-support/05-01-SUMMARY.md` when done.
</output>

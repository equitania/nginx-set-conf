---
phase: "02-cache-template-substitution-consolidation"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - nginx_set_conf/validators.py
  - tests/test_validators.py
autonomous: true
requirements:
  - COR-04

must_haves:
  truths:
    - "VALID_TEMPLATES in validators.py has an inline comment explaining WHY default_ssl_reject is excluded"
    - "default_ssl_reject is NOT in VALID_TEMPLATES (exclusion is intentional, not an omission)"
    - "ngx_default_ssl_reject is NOT in VALID_TEMPLATES_COMPAT"
    - "A regression test asserts the exclusion so future additions are deliberate"
    - "Full test suite green"
  artifacts:
    - path: "nginx_set_conf/validators.py"
      provides: "4-line comment block above VALID_TEMPLATES explaining default_ssl_reject exclusion"
      contains: "default_ssl_reject"
    - path: "tests/test_validators.py"
      provides: "Two regression guard methods in TestValidateConfigTemplate"
      contains: "test_default_ssl_reject_not_in_valid_templates"
  key_links:
    - from: "nginx_set_conf/validators.py"
      to: "nginx_set_conf/utils.py"
      via: "validate_config_template → VALID_TEMPLATES lookup; setup_default_server bypasses this path"
      pattern: "VALID_TEMPLATES"
---

<objective>
Document WHY default_ssl_reject is absent from VALID_TEMPLATES by adding an inline
comment block directly above the set definition in validators.py. Add two regression
guard tests to TestValidateConfigTemplate ensuring the exclusion remains intentional
and is not accidentally reversed.

Purpose: The absence of default_ssl_reject from VALID_TEMPLATES is correct behaviour
(it is generated only by --setup_default / setup_default_server, not by --config_template),
but there is no inline rationale. Future operators or contributors may file phantom bugs
or accidentally add it to the whitelist. The comment closes the documentation gap
(COR-LOW-1) and the regression tests lock the decision in place.

Output:
- nginx_set_conf/validators.py — 4-line comment block above VALID_TEMPLATES (no code change)
- tests/test_validators.py — 2 new test methods inside TestValidateConfigTemplate
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NSC-02-cache-template-substitution-consolidation/02-RESEARCH.md
</context>

<interfaces>
From nginx_set_conf/validators.py (current state — read before editing):

  VALID_TEMPLATES definition (lines 33-51):
    # Valid template names (whitelist)
    VALID_TEMPLATES = {
        "code_server",
        "fast_report",
        ... (17 entries)
        "supabase",
    }
    "default_ssl_reject" is ABSENT — intentionally.

  VALID_TEMPLATES_COMPAT (line 54):
    VALID_TEMPLATES_COMPAT = {f"ngx_{t}" for t in VALID_TEMPLATES}
    "ngx_default_ssl_reject" is also absent — correct.

  Comment to add immediately before "VALID_TEMPLATES = {":
    Replace the existing "# Valid template names (whitelist)" with an expanded block:

    # VALID_TEMPLATES: template names accessible via --config_template / YAML config_template.
    #
    # `default_ssl_reject` is intentionally excluded: it is generated only by --setup_default
    # (setup_default_server in utils.py) which writes to 00-default.conf using a sacrificial
    # self-signed cert. Exposing it via --config_template would allow misuse as a regular
    # vhost config and could overwrite the SNI catch-all. See CONCERNS.md §COR-LOW-1.
    VALID_TEMPLATES = {

From nginx_set_conf/utils.py (for context only — not edited in this plan):
  setup_default_server (lines 483-509) is the only code path that generates
  a default_ssl_reject config. It writes to 00-default.conf directly.

From tests/test_validators.py:
  TestValidateConfigTemplate is the existing class that tests validate_config_template.
  New methods are appended to this class (not a new class).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add explanatory comment above VALID_TEMPLATES (COR-04)</name>
  <files>nginx_set_conf/validators.py</files>

  <read_first>
    1. nginx_set_conf/validators.py — read lines 30-55; note the exact current comment
       ("# Valid template names (whitelist)") that precedes VALID_TEMPLATES. Confirm
       line numbers so the Edit replaces only that comment line, not the set body.
    2. nginx_set_conf/validators.py — confirm the format of other multi-line comments
       in the file (# prefix, no block-style). Match that convention.
  </read_first>

  <action>
    Edit nginx_set_conf/validators.py.

    Find the line: "# Valid template names (whitelist)"
    Replace it with the following 5-line comment block (keeping consistent # prefix style):

      # VALID_TEMPLATES: template names accessible via --config_template / YAML config_template.
      #
      # `default_ssl_reject` is intentionally excluded: it is generated only by --setup_default
      # (setup_default_server in utils.py) which writes to 00-default.conf using a sacrificial
      # self-signed cert. Exposing it via --config_template would allow misuse as a regular
      # vhost config and could overwrite the SNI catch-all. See CONCERNS.md §COR-LOW-1.

    The VALID_TEMPLATES = { line immediately follows, unchanged.
    No other lines in the file are modified.

    Commit message: [CHG] docs(NSC-02-03): COR-04 — document default_ssl_reject exclusion from VALID_TEMPLATES
  </action>

  <verify>
    <automated>
      cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && grep -n "default_ssl_reject" nginx_set_conf/validators.py
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c "default_ssl_reject" nginx_set_conf/validators.py returns at least 1
    - The matching line(s) are comment lines only (start with #) — no code adds default_ssl_reject to the set
    - grep -n "Valid template names" nginx_set_conf/validators.py returns 0 (old single-line comment replaced)
    - grep -n "VALID_TEMPLATES: template names" nginx_set_conf/validators.py returns 1
    - nginx_set_conf/validators.py — VALID_TEMPLATES set body is unchanged (17 entries, same as before)
    - uv run python -c "from nginx_set_conf.validators import VALID_TEMPLATES; assert 'default_ssl_reject' not in VALID_TEMPLATES" exits 0
  </acceptance_criteria>

  <done>
    Multi-line explanatory comment present above VALID_TEMPLATES. Exclusion of
    default_ssl_reject is now documented inline with rationale pointing to CONCERNS.md.
    No code behaviour changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression guard tests for default_ssl_reject exclusion (COR-04)</name>
  <files>tests/test_validators.py</files>

  <read_first>
    1. tests/test_validators.py — read the full file; locate the existing class
       TestValidateConfigTemplate. Note its last method (append new methods after it,
       inside the same class — not a new class).
    2. nginx_set_conf/validators.py (post-Task-1) — confirm VALID_TEMPLATES and
       VALID_TEMPLATES_COMPAT are importable from the module.
  </read_first>

  <behavior>
    After this task:
    - Two new methods are appended inside TestValidateConfigTemplate (not a separate class).
    - test_default_ssl_reject_not_in_valid_templates:
        imports VALID_TEMPLATES; asserts "default_ssl_reject" not in VALID_TEMPLATES.
    - test_default_ssl_reject_not_in_compat_templates:
        imports VALID_TEMPLATES_COMPAT; asserts "ngx_default_ssl_reject" not in VALID_TEMPLATES_COMPAT.
    - Both tests fail if someone accidentally adds default_ssl_reject to the whitelist.
    - uv run pytest tests/test_validators.py::TestValidateConfigTemplate -v --no-cov exits 0.
  </behavior>

  <action>
    Append two methods to the existing TestValidateConfigTemplate class in
    tests/test_validators.py, after its last existing method. Use 4-space indent
    consistent with the class.

    Method descriptions (implement as standard pytest methods):

    def test_default_ssl_reject_not_in_valid_templates(self):
      from nginx_set_conf.validators import VALID_TEMPLATES
      assert "default_ssl_reject" not in VALID_TEMPLATES, (
          "default_ssl_reject must remain excluded from VALID_TEMPLATES — "
          "it is only generated by --setup_default. See CONCERNS.md §COR-LOW-1."
      )

    def test_default_ssl_reject_not_in_compat_templates(self):
      from nginx_set_conf.validators import VALID_TEMPLATES_COMPAT
      assert "ngx_default_ssl_reject" not in VALID_TEMPLATES_COMPAT, (
          "ngx_default_ssl_reject must not appear in VALID_TEMPLATES_COMPAT "
          "since default_ssl_reject is intentionally excluded from VALID_TEMPLATES."
      )

    Commit message: [ADD] tests(NSC-02-03): COR-04 regression guard — default_ssl_reject excluded from VALID_TEMPLATES
  </action>

  <verify>
    <automated>
      cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf && uv run pytest tests/test_validators.py::TestValidateConfigTemplate -v --no-cov
    </automated>
  </verify>

  <acceptance_criteria>
    - tests/test_validators.py contains "test_default_ssl_reject_not_in_valid_templates"
    - tests/test_validators.py contains "test_default_ssl_reject_not_in_compat_templates"
    - Both methods are inside class TestValidateConfigTemplate (not a new class)
    - uv run pytest tests/test_validators.py::TestValidateConfigTemplate -v --no-cov exits 0
      with both new methods shown as PASSED
    - uv run pytest tests/ -q exits 0 (full suite green, coverage gate met)
  </acceptance_criteria>

  <done>
    Both regression guard methods present and passing inside TestValidateConfigTemplate.
    Full suite green. The COR-04 decision is locked: default_ssl_reject stays excluded
    from VALID_TEMPLATES and any future accidental addition is caught by tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --config_template CLI input → validate_config_template | Operator-supplied template name; default_ssl_reject must never be routable via this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Phantom whitelist addition — default_ssl_reject accidentally added to VALID_TEMPLATES | mitigate | Regression tests assert exclusion; inline comment explains rationale pointing to CONCERNS.md; future PR reviewers have clear signal |
| T-02-03-02 | Elevation of Privilege | Operator uses --config_template default_ssl_reject to overwrite 00-default.conf SNI catch-all with wrong cert | accept | Current code already rejects the template name via validate_config_template; this plan adds documentation and regression tests, not new runtime protection. The runtime protection was already present. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no new dependencies |
</threat_model>

<verification>
Full suite:

  cd /Users/picard/gitbase/PyPi-Projects/nginx-set-conf
  uv run pytest tests/ -q

Targeted regression:

  uv run pytest tests/test_validators.py::TestValidateConfigTemplate -v --no-cov

Structural assertions:

  grep -n "default_ssl_reject" nginx_set_conf/validators.py
  # Expected: comment lines only, 0 set entries

  grep -c "VALID_TEMPLATES: template names" nginx_set_conf/validators.py
  # Expected: 1
</verification>

<success_criteria>
1. nginx_set_conf/validators.py — 5-line comment block present above VALID_TEMPLATES
   explaining default_ssl_reject exclusion with reference to CONCERNS.md §COR-LOW-1.
   The single-line "# Valid template names (whitelist)" is replaced.
2. tests/test_validators.py — test_default_ssl_reject_not_in_valid_templates and
   test_default_ssl_reject_not_in_compat_templates added inside TestValidateConfigTemplate.
3. uv run pytest tests/test_validators.py::TestValidateConfigTemplate -v --no-cov exits 0.
4. uv run pytest tests/ -q exits 0 (full suite green, coverage gate met).
5. pyproject.toml version, nginx_set_conf/__init__.py __version__, and RELEASE_NOTES.md
   are NOT touched (Phase 4 responsibility).
</success_criteria>

<output>
Create `.planning/phases/NSC-02-cache-template-substitution-consolidation/02-03-SUMMARY.md` when done.
</output>
